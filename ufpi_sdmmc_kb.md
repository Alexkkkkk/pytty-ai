# UFPI: работа с SD/eMMC (модуль знаний для ИИ)

По мотивам раздела SD/MMC форума mslw.com/bb (fid=120, UFPI Support Forum),
официального мануала UFPI, инструкций muuh.ru, тем televid-sib.org и
kenotrontv.ru. UFPI (Universal Flash Programming Interface) — программатор
MSL Works: JTAG, SPI, I2C, NAND, eMMC, SD, 1-Wire; скрипты C-подобного
синтаксиса; внешние конфиги для «неподдержанных» чипов.

## Подключение eMMC по ISP (без выкуса)

Обязательные сигналы: **CMD, CLK, DAT0, GND + питание VCC и VCCQ**.
Полный ISP-набор: DAT0–DAT3(+DAT4-7 для 8-bit), CMD, CLK, RSTN(по
возможности). Питание можно подавать от UFPI («кнопка молнии» + свои
напряжения) или внешнее. VCCQ=1.8V — для 4-bit режима при прошивке
включать опцию «4-Бит режим при VCCQ=1V8» (стабильность).

### Траблшутинг «не коннектится» (типовой порядок)
1. **CMD1 Get Response Error / R3 Response Check Bits Error** — чип в
   standby не выходит: проверить питание, RSTN, CLK; часто помогает
   подтяжка RSTN, сброс питания (Power Cycle), режим 1-bit, Slow Timing 8 MHz.
2. **Линии молчат** — плата держит шину: SoC не в reset → подать reset
   на проц (удержание кнопки/линии) или выпаивать чип.
3. **Ошибки чтения на исправной флеш** — уменьшить частоту (52→26 MHz),
   включить «Одиночными блоками», «Игнорировать ошибки чтения», снизить
   до 1 bit. Задержки семплирования/выхода — выставить максимум, потом
   понижать до стабильного.
4. **«DAT0 Заблокировано!»** — eMMC с паролем: Разблокировать (пароль
   ASCII) → работа → Отключить сессию; пароль неизвестен — «Стереть»
   (стирает и содержимое) или «Брутфорс».
5. **Тест контактов** (Pin Test) — перед работой, выявить обрывы/коротыше.
6. Читается ID, но ошибки в User — режим чтения «Одиночными блоками» +
   «Игнорировать ошибки» + количество повторов в настройках модуля
   (вычитка неисправных eMMC с пропуском битых блоков).

### Скорости ISP (опыт televid-sib)
- 52 MHz + асинхронный режим: чтение 4 ГБ ≈ 3 мин, запись ≈ 7 мин.
- HS200 до 90 MHz в распространённых BGA-колодках нестабилен — не гнаться.
- При изношенной eMMC и ошибках CRC — полезна функция «продолжить
  чтение/запись с прерванного места» (запрос в разделе fid=120).

## Чтение / запись / стирание: режимы (UFPI)

- **Чтение**: Автовыбор (исправные, колодка); Одиночными блоками (CMD17,
  контроль каждого блока — для больных флеш, самый медленный);
  Многоблок (открытый/счётчик CMD23/один запрос CMD18). «Игнорировать
  ошибки» — не останавливаться, читать дальше (больные флеш).
- **Запись**: аналогично; 4-bit при VCCQ 1.8V — включать для стабильности.
- **Стирание**: Авто (по eMMC); одиночными/многоблоками; «Запись нулей».
- **Асинхронный режим**: выше скорость, НО чтение с пропуском ошибок в
  нём невозможно — для больных флеш отключать.

## Регистры и конфигурация eMMC

- **eCSD** (512 байт): при чтении ID каждый байт подписан с расшифровкой;
  запись по номеру байта или целиком файлом. Не все регистры доступны
  для записи/восстановления (зависит от вендора, смотреть JEDEC).
- **Ключевые байты при замене флеш**: [162] RST_N (OTP — однократно!),
  [177] boot config, [179] PARTITION_CONFIG (калькулятор «Конфиг
  загрузки»: выбор загрузки с boot0/boot1/user). После замены eMMC
  обязательно выставить как у родной (считать ID родной ДО выпаивания!).
- **CID**: 16 байт (MID, PNM, PSN, дата). На Samsung ≤v5.0 доступна
  «Запись CID» (даже от чужого вендора) — нужно, когда имя чипа хранится
  в загрузчике и проверяется.
- **CSD**: биты 12/13 — PERM_WRITE_PROTECT / TMP_WRITE_PROTECT. PERM
  необратим; такие чипы лечит «Форматирование» или прошивка внутреннего
  FW, но надёжность под вопросом.
- **RPMB**: 32-байтный ключ аутентификации HEX; записывается один раз,
   необратимо. Sony TV: ключи DRM (Netflix/HDCP) в RPMB — теряются навсегда.
- **Логи**: `Logs/eMMC/` — eCSD/CID/CSD файлы каждого подключённого чипа
  (дата/время, номер флеш) — ничего не теряется, полезно для сравнения.

## Специфика вендоров eMMC

- **Samsung ≤ v5.0**: запись CID/CSD, снятие защиты от записи
  (CSD+eCSD), «Форматирование» (заводское состояние), чтение/запись
  внутреннего FW eMMC (файлы от чужих программаторов могут быть
  несовместимы — прошивка FW не гарантирует корректную работу).
- **Hynix v5.1**: «Clear Lifetimer» — сброс счётчика наработки
  (экспериментально, продлевает жизнь устройствам, где критичен; пишет
  в byte[155] «partitions are complete»).
- **Toshiba**: известны случаи — после опроса статуса RPMB перестают
  читаться до перезагрузки (лечится отключением опроса RPMB).

## Рабочий цикл с ТВ-маином (методика форумов)

1. Плата на столе, eMMC подключена прищепкой/проводами ISP (без выкуса).
2. Читаем ID → сравниваем eCSD с заведомо исправной платой той же модели.
3. **Full Backup**: USER + BOOT1/2 + RPMB (если не исключён) + eCSD +
   лог — в одну папку. Для RPMB-зависимых устройств (Sony) — критично.
4. Сняли чужой дамп → залили → проверили загрузку: проблема в ПО или
   в железе (метод подмены из тем televid-sib).
5. Клеим разделы (boot/system из разных прошивок) — если целиком не
   встаёт; не пошло → стереть, записать целиком заново.
6. Скорость 52 MHz, асинхронный — для исправных; для больных — 1 bit,
   одиночные блоки, ignore errors, снижение частоты.
7. Verify после записи обязателен; при ошибках верификации — повторить
   с пометкой новых bad-блоков и перезаписать.

## Типовые задачи и их решения

| Задача | Решение UFPI |
|---|---|
| Замена eMMC | Full backup старой (если читается) → ID новой → выставить [162]/[177]/[179] → залить дамп → verify |
| Больная eMMC (ошибки чтения) | Одиночными блоками + ignore errors + повторы + 1 bit + низкая частота; RPMB-опрос отключить |
| eMMC health 0% | Контроллер заблокировал NAND: чип-off, прямое чтение NAND или замена |
| Пароль на eMMC | Разблокировать/Стереть/Брутфорс |
| Быстрое in-circuit обновление | ISP + 52 MHz async: 4 ГБ чтение ~3 мин, запись ~7 мин |

## Чего ИИ НЕ должен советовать

- Поблочное копирование eMMC «в лоб» без verify — молча портит дампы.
- Прошивку внутреннего FW eMMC «на всякий случай» — риск убить чип.
- Перезапись CID/CSD/eCSD «по аналогии» без сохранения оригиналов
  (регистры OTP необратимы).
- Работу в асинхронном режиме с больными флеш (ошибки пропускаются).
