# user_patches.py — код, сгенерированный ИИ (самопереписывание программы).
# Функция on_output(text) вызывается на каждый вывод терминала.
# Верните строку — она попадёт в журнал ИИ. Ошибки игнорируются.

def on_output(text):
    # Пример: предупредить при появлении ошибки подписи
    if "Bad signature" in text:
        return "Обнаружена ошибка подписи — проверьте флешку (FAT32/MBR)"
    return None
